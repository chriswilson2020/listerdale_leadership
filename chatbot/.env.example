# Required: Your OpenAI API key
OPENAI_API_KEY=sk-your-api-key-here

# Optional: OpenAI model to use (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: Port to run the server on (default: 3001)
PORT=3001

# Optional: Allowed origins for CORS (comma-separated, default: * allows all)
# For production, set this to your actual domain:
# ALLOWED_ORIGINS=https://listerdalestrategy.com,https://www.listerdalestrategy.com
ALLOWED_ORIGINS=*
